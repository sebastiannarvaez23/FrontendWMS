import { createSlice } from "@reduxjs/toolkit";

const initialState = {

}

export const pickingSlice = createSlice({
    name: "picking",
    initialState,
    reducers: {

    }
})

export const {  } = pickingSlice.actions;
export default pickingSlice.reducer;